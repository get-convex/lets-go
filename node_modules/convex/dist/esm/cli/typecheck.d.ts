import { Command } from "commander";
export declare type TypecheckResult = "cantTypeCheck" | "success" | "typecheckFailed";
/** Run the TypeScript compiler, as configured during  */
export declare const typecheck: Command;
export declare type TypeCheckMode = "enable" | "try" | "disable";
/**
 * Conditionally run a typecheck function and interpret the result.
 *
 * If typeCheckMode === "disable", never run the typecheck function.
 * If typeCheckMode === "enable", run the typecheck and crash if typechecking
 * fails or we can't find tsc.
 * If typeCheckMode === "try", try and run the typecheck. crash if typechecking
 * fails but don't worry if tsc is missing and we can't run it.
 */
export declare function processTypeCheckResult(typeCheckMode: TypeCheckMode, runTypeCheck: () => Promise<TypecheckResult>): Promise<void>;
export declare function typeCheckFunctions(functionsDir: string): Promise<TypecheckResult>;
export declare function doTypeCheck(entryPoints: string[]): Promise<TypecheckResult>;
//# sourceMappingURL=typecheck.d.ts.map